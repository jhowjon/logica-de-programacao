const ingles: boolean = true;
const espanhol: boolean = false;

if (ingles || espanhol) {
  const bilingue: string = "É Bilingue!!!!";
  console.log(bilingue);
} else {
  const naoEhBilingue: string = "Não é Bilingue !!!!";
  console.log(naoEhBilingue);
}
